# 运行时数据(数据库、密钥、临时文件、git 仓库克隆)
data/

# Python
.venv/
__pycache__/
*.pyc

# Node / 前端构建
node_modules/
# 注意: frontend/dist 有意纳入 git 管理,服务器自动更新直接拉取即可运行。
# 提交代码前务必先执行 cd frontend && npm run build 保证 dist 最新。

# 编辑器与系统
.idea/
.vscode/
.DS_Store
Thumbs.db
